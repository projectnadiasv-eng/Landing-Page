/* STUB — to be replaced by the block agent that owns src/components/Footer/.
 * Port target: legacy/index.html 4831-5088
 * See docs/OWNERSHIP.md before editing anything outside this directory. */
export default function Footer() {
  return <footer id="spft-root" />
}
