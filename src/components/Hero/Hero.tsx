/* STUB — to be replaced by the block agent that owns src/components/Hero/.
 * Port target: legacy/index.html 176-692
 * See docs/OWNERSHIP.md before editing anything outside this directory. */
export default function Hero() {
  return <div id="sehx2-root" />
}
