/* STUB — to be replaced by the block agent that owns src/components/Preloader/.
 * Port target: legacy/index.html 15-173
 * See docs/OWNERSHIP.md before editing anything outside this directory. */
export default function Preloader() {
  return <div id="sploader" />
}
